import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Financial Model 2",
  description: "Seeded OPSL plantation financial model test app.",
  icons: {
    icon: "/fm/public/agrinexus-favicon-32.png?v=6",
    shortcut: "/fm/public/agrinexus-favicon-32.png?v=6",
    apple: "/fm/public/apple-touch-icon.png?v=6",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
